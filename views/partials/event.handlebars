<div class="card">
    <div class="card-block">
        <h2 class="card-title">{{this.name}}</h2><br>
        <div class="card-text">
            {{this.description}}
            {{this.date}}
            {{this.location}}
        </div>
    </div>
</div>
