<div class="col-lg-4" id="agenda">
    <h4>Name: {{events.name}}</h4>
    <p>
        Description: {{events.description}}
        {{events.date}}
    </p></br>
</div>

<div class="col-lg-4" id="stops">
    <h3>{{events.stops}}
</div>
